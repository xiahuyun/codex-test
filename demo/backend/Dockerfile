FROM golang:1.22-alpine AS builder
WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /snake-server ./cmd/server

FROM alpine:3.20
WORKDIR /app
COPY --from=builder /snake-server /usr/local/bin/snake-server
EXPOSE 8080
CMD ["snake-server"]
